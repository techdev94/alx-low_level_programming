#include "main.h"

/**
 * int mul(int a, int b) - function that multiplies two integers
 * a nd b contains integers to be multiplied
 * Return: Always 0.
 */
int mul(int a, int b)

{
return (a*b);
}
